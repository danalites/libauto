---
 task: quick-time-screen-recorder
 
 actions:
   # Run QT screen recorder, select screen region and start recording
   - os.shell(quick-time-start-record.applescript)

   # Press Esc when you finished recording
   - key.wait(Esc)
   - os.shell(quick-time-stop-record.applescript)